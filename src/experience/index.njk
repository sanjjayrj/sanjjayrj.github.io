---
layout: base.njk
title: Experience
description: Professional experience, education, skills, and certifications.
extraCss:
  - /css/sections.css
---

<div class="experience-detail">
  <div class="container">
    <a href="/#experience" class="post-back">Back to Home</a>

    <h1 class="section-title" style="margin-top: 24px;">experience</h1>

    <div class="timeline">
      {% for job in experience.jobs %}
      <div class="timeline-item">
        <h3 class="timeline-role">{{ job.role }}</h3>
        <p class="timeline-company">{{ job.company }} &middot; {{ job.period }}</p>
        <ul class="timeline-desc">
          {% for bullet in job.bullets %}
          <li>{{ bullet }}</li>
          {% endfor %}
        </ul>
      </div>
      {% endfor %}
    </div>

    <h2 class="section-title" style="margin-top: 60px;">education</h2>

    <div class="education-grid">
      {% for edu in education.entries %}
      <div class="education-card">
        <h4>{{ edu.degree }}</h4>
        <p class="institution">{{ edu.institution }}</p>
        <p class="details">{{ edu.period }}{% if edu.gpa %} &middot; GPA: {{ edu.gpa }}{% endif %}</p>
      </div>
      {% endfor %}
    </div>

    <h2 class="section-title" style="margin-top: 60px;">skills</h2>

    <div class="skills-section">
      {% for group in skills.groups %}
      <div class="skills-group">
        <h4>{{ group.category }}</h4>
        <div class="skills-list">
          {% for skill in group.items %}
          <span class="tag">{{ skill }}</span>
          {% endfor %}
        </div>
      </div>
      {% endfor %}
    </div>

    <h2 class="section-title" style="margin-top: 60px;">certifications</h2>

    <div class="certifications-list">
      {% for cert in certifications.items %}
      <div class="certification-card">
        <h3>{{ cert.name }}</h3>
        <p>{{ cert.issuer }} &middot; {{ cert.year }}</p>
        {% if cert.url %}
        <a href="{{ cert.url }}" target="_blank" rel="noopener" class="card-link">View credential</a>
        {% endif %}
      </div>
      {% endfor %}
    </div>
  </div>
</div>
